// DailyPhotoSectionPage — placeholder (will be replaced in Task 3)
export default function DailyPhotoSectionPage() {
  return (
    <div className="page">
      <p style={{ fontFamily: 'var(--font-hand)', fontSize: '1rem', color: 'var(--text-secondary)', textAlign: 'center', paddingTop: 64 }}>
        loading...
      </p>
    </div>
  )
}
