---
phase: 09-usecallback-hook-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/PredictPartnerPage.jsx
  - src/pages/StudyTogetherPage.jsx
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "PredictPartnerPage.fetchResponses is wrapped in useCallback with [sessionId] dependency"
    - "StudyTogetherPage.fetchData is wrapped in useCallback with [sessionId] dependency"
    - "useRealtimeSync polling interval stays stable at 5 seconds without resetting on parent re-renders"
  artifacts:
    - path: "src/pages/PredictPartnerPage.jsx"
      provides: "useCallback-wrapped fetchResponses"
      contains: "useCallback"
    - path: "src/pages/StudyTogetherPage.jsx"
      provides: "useCallback-wrapped fetchData"
      contains: "useCallback"
  key_links:
    - from: "src/pages/PredictPartnerPage.jsx"
      to: "src/hooks/useRealtimeSync.js"
      via: "onUpdate callback reference stability"
      pattern: "useCallback.*fetchResponses"
    - from: "src/pages/StudyTogetherPage.jsx"
      to: "src/hooks/useRealtimeSync.js"
      via: "onUpdate callback reference stability"
      pattern: "useCallback.*fetchData"
---

<objective>
Wrap fetchResponses (PredictPartnerPage) and fetchData (StudyTogetherPage) in useCallback so useRealtimeSync receives a stable function reference, preventing its polling and realtime effects from re-subscribing on every parent render.

Purpose: Closes QUAL-03 — the last remaining audit finding. Without useCallback, every render creates a new function reference, causing useRealtimeSync to tear down and recreate the polling interval and realtime channel.

Output: Two modified page files with stable fetch callbacks.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useRealtimeSync.js
@src/pages/PredictPartnerPage.jsx
@src/pages/StudyTogetherPage.jsx

<interfaces>
<!-- useRealtimeSync expects onUpdate to be a stable callback reference -->
From src/hooks/useRealtimeSync.js:
```javascript
// Both effects depend on onUpdate in their dep arrays:
useEffect(() => { /* realtime */ }, [sessionId, table, onUpdate])
useEffect(() => { /* polling */ }, [sessionId, onUpdate, pollingEnabled, pollingInterval])
```

<!-- Established pattern from LoveNoteHuntPage (Phase 8): -->
From src/pages/LoveNoteHuntPage.jsx:
```javascript
const handleWaitingUpdate = useCallback(async () => {
  // ... fetch logic using mountedRef, sessionId ...
}, [sessionId, /* other stable deps */])
```

<!-- useSessionSetup returns refs (mountedRef) and stable primitives (sessionId, playerId) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap fetchResponses and fetchData in useCallback</name>
  <files>src/pages/PredictPartnerPage.jsx, src/pages/StudyTogetherPage.jsx</files>
  <action>
In PredictPartnerPage.jsx:
1. Add `useCallback` to the React import (line 1): `import { useState, useEffect, useRef, useCallback } from 'react'`
2. Wrap the `fetchResponses` function (line 41) in useCallback with `[sessionId]` as the dependency array. The function uses `supabase` (module-level import, stable), `mountedRef` (ref, stable), `sessionId` (primitive from useSessionSetup), and state setters (`setAllResponses`, `setLoading` — stable by React guarantee). Only `sessionId` needs to be in the dep array.
3. Update the initial fetch useEffect (line 75) to include `fetchResponses` in its dependency array: `useEffect(() => { fetchResponses() }, [fetchResponses])` — this is safe because useCallback makes the reference stable across renders when sessionId hasn't changed.

In StudyTogetherPage.jsx:
1. Add `useCallback` to the React import (line 1): `import { useState, useEffect, useRef, useCallback } from 'react'`
2. Wrap the `fetchData` function (line 52) in useCallback with `[sessionId]` as the dependency array. The function uses `supabase` (module-level, stable), `mountedRef` (ref, stable), `sessionId` (primitive), and state setters (`setData`, `setLoading` — stable). Only `sessionId` needs to be in the dep array.
3. Update the initial fetch useEffect (line 88) to include `fetchData` in its dependency array: `useEffect(() => { fetchData() }, [fetchData])`

Do NOT change anything else in either file. The useRealtimeSync calls already pass the fetch functions as onUpdate — no changes needed there.
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
Both fetchResponses and fetchData are wrapped in useCallback with [sessionId] dependency. The useRealtimeSync onUpdate references are now stable across renders, so polling intervals and realtime subscriptions no longer reset on every parent state change. Production build succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no other useRealtimeSync consumers have unwrapped callbacks</name>
  <files>src/pages/PredictPartnerPage.jsx, src/pages/StudyTogetherPage.jsx</files>
  <action>
Run a grep across all files that call useRealtimeSync to confirm every onUpdate callback is wrapped in useCallback. The check should:
1. Find all files importing useRealtimeSync
2. For each file, confirm the function passed as onUpdate is defined with useCallback
3. If any file passes a bare function (not useCallback-wrapped), flag it — but based on Phase 8 work, PredictPartnerPage and StudyTogetherPage are the only two remaining unwrapped consumers

This is a verification-only task — no code changes unless an unexpected unwrapped consumer is found. If one is found, wrap it following the same pattern (useCallback with [sessionId] dep).
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && grep -rl "useRealtimeSync" src/ | xargs grep -L "useCallback" | grep -v node_modules; echo "EXIT:$?"</automated>
  </verify>
  <done>
Every file that uses useRealtimeSync also imports and uses useCallback for its onUpdate function. Zero files remain with unwrapped fetch callbacks. The grep returns no results (all consumers compliant).
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` completes with no errors
2. `grep -rl "useRealtimeSync" src/ | xargs grep -L "useCallback"` returns empty (all consumers use useCallback)
3. In both modified files, the useCallback dependency array contains `[sessionId]` and no unnecessary deps
</verification>

<success_criteria>
- PredictPartnerPage.fetchResponses is defined with useCallback([sessionId])
- StudyTogetherPage.fetchData is defined with useCallback([sessionId])
- No useRealtimeSync consumer passes an unwrapped callback
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-usecallback-hook-compliance/09-01-SUMMARY.md`
</output>
