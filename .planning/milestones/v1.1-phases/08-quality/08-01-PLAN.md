---
phase: 08-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/VisionTab.jsx
  - src/index.css
  - src/utils/sessionUtils.js
  - src/utils/sessionUtils.test.js
autonomous: true
requirements:
  - QUAL-01
  - TEST-01

must_haves:
  truths:
    - "VisionTab pin hover effects work without any direct DOM style mutations"
    - "The test suite runs with zero failures"
    - "The isTabActive function comment references /study, not /books"
  artifacts:
    - path: "src/pages/VisionTab.jsx"
      provides: "State-driven hover effects on cork board pins"
      contains: "vision-pin"
    - path: "src/index.css"
      provides: "CSS hover class for vision board pins"
      contains: "vision-pin"
    - path: "src/utils/sessionUtils.test.js"
      provides: "Fixed route references"
      contains: "/study/abc"
    - path: "src/utils/sessionUtils.js"
      provides: "Fixed comment referencing /study"
      contains: "/study"
  key_links:
    - from: "src/pages/VisionTab.jsx"
      to: "src/index.css"
      via: "CSS class name"
      pattern: "vision-pin"
---

<objective>
Replace VisionTab direct DOM style mutations with CSS-driven hover effects, and fix stale /books route references in test suite.

Purpose: Eliminate imperative DOM manipulation anti-pattern flagged by audit; restore green test suite.
Output: Clean VisionTab hover behavior via CSS class, all tests passing.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/VisionTab.jsx
@src/index.css
@src/utils/sessionUtils.js
@src/utils/sessionUtils.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace VisionTab DOM mutations with CSS hover class</name>
  <files>src/pages/VisionTab.jsx, src/index.css</files>
  <action>
1. Add a CSS class to `src/index.css` for the vision board pin hover effect:

```css
.vision-pin {
  transition: transform 0.15s;
}
.vision-pin:hover {
  transform: scale(1.3);
}
.vision-pin--disabled:hover {
  transform: none;
}
```

2. In `src/pages/VisionTab.jsx`, at line ~320 (empty-state pins inside the board):
   - Add `className="vision-pin"` to the div
   - Remove the `onMouseEnter` handler (line 335)
   - Remove the `onMouseLeave` handler (line 336)
   - Remove the inline `transition: 'transform 0.15s'` style (line 332) since CSS handles it

3. In `src/pages/VisionTab.jsx`, at line ~875 (CorkBoardSlot pin):
   - Add `className={hasPhoto ? 'vision-pin vision-pin--disabled' : 'vision-pin'}` to the div
   - Remove the `onMouseEnter` handler (line 888)
   - Remove the `onMouseLeave` handler (line 889)
   - Remove the inline `transition: 'transform 0.15s'` style (line 886) since CSS handles it

There are exactly 4 DOM mutation lines (335, 336, 888, 889) — all must be removed. The conditional `hasPhoto` logic on the second pair is handled by the `--disabled` modifier class.
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && grep -n "\.style\.\(transform\|opacity\|scale\)" src/pages/VisionTab.jsx | wc -l</automated>
    Expect: 0 (no direct DOM style mutations remain in VisionTab)
  </verify>
  <done>All 4 onMouseEnter/Leave DOM mutation lines removed from VisionTab.jsx. Hover effects driven by CSS class. No behavioral change — pins still scale on hover, disabled pins do not.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale /books route references in test suite and source comment</name>
  <files>src/utils/sessionUtils.test.js, src/utils/sessionUtils.js</files>
  <action>
1. In `src/utils/sessionUtils.test.js`:
   - Line 57: Change `['/books/abc', 'Books — The Us Quiz']` to `['/study/abc', 'Study Together — The Us Quiz']` (the `getDocumentTitle` function already returns 'Study Together — The Us Quiz' for `/study` paths)
   - Lines 139-141: Change the `isTabActive` test from:
     ```
     it('is active on /books', () => {
       expect(isTabActive(tabPath, '/books/abc')).toBe(true)
     })
     ```
     to:
     ```
     it('is active on /study', () => {
       expect(isTabActive(tabPath, '/study/abc')).toBe(true)
     })
     ```
     Note: `/study` routes are in the "us" tab (profiles), not "fun stuff". So this test case should actually be MOVED from the "fun stuff tab" describe block to the "us tab" describe block, OR simply updated to test that `/study` is NOT active on the fun tab (expect false) and add a new test in the "us tab" block. Check the `isTabActive` implementation: `/study` is under `/profiles` base — so fun tab should NOT match `/study`. The correct fix:
     - In the "fun stuff" describe block: change the test to verify `/study` is NOT active: `expect(isTabActive(tabPath, '/study/abc')).toBe(false)`
     - In the "us tab" describe block: add a new test: `it('is active on /study', () => { expect(isTabActive(tabPath, '/study/abc')).toBe(true) })`

2. In `src/utils/sessionUtils.js`:
   - Line 52 comment: Change `/books` to `/study` in the isTabActive JSDoc comment (the fun stuff grouping list)
   - Actually check: the comment at line 52 lists `/books` under fun stuff tab. The route was renamed to `/study` and moved to the "us" tab. Update the comment to remove `/books` from the fun stuff list and add `/study` to the profiles/us list:
     ```
     *  - "/fun/:id" (fun stuff) → /fun, /draw, /movies, /watch-guide, /tictactoe, /love-notes
     *  - "/profiles/:id" (us)   → /profiles, /journal, /study, /personality, /vision
     ```
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && npm test 2>&1</automated>
    Expect: All tests pass (0 failures)
  </verify>
  <done>Test suite passes with zero failures. No references to /books route remain in sessionUtils.test.js or sessionUtils.js. Route groupings in JSDoc comment match actual isTabActive implementation.</done>
</task>

</tasks>

<verification>
1. `grep -rn '/books' src/utils/sessionUtils*` returns no matches
2. `grep -n '\.style\.' src/pages/VisionTab.jsx` returns no direct DOM style mutations (only inline style objects are fine)
3. `npm test` — all tests pass
4. `npm run build` — production build succeeds
</verification>

<success_criteria>
- Zero direct DOM style mutations (`.style.transform`) in VisionTab.jsx
- Pin hover effects still work visually (CSS-driven)
- Test suite: 0 failures (was 2 failures from stale /books references)
- isTabActive JSDoc comment matches implementation
</success_criteria>

<output>
After completion, create `.planning/phases/08-quality/08-01-SUMMARY.md`
</output>
