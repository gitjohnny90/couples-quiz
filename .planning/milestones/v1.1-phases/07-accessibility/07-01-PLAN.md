---
phase: 07-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/VaultPage.jsx
  - src/pages/HotTakesPage.jsx
  - src/pages/VisionTab.jsx
  - src/pages/StudyTogetherPage.jsx
  - src/pages/ResultsPage.jsx
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Every interactive card on VaultPage is reachable via Tab and activatable via Enter or Space"
    - "Every group card on HotTakesPage categories screen is reachable via Tab and activatable via Enter or Space"
    - "Category headers in VisionTab dreams section are reachable via Tab and activatable via Enter or Space"
    - "Book cards on StudyTogetherPage are reachable via Tab and activatable via Enter or Space"
    - "Reveal cards on ResultsPage are reachable via Tab and activatable via Enter or Space"
  artifacts:
    - path: "src/pages/VaultPage.jsx"
      provides: "Keyboard-accessible quiz type cards"
      contains: "role=\"button\""
    - path: "src/pages/HotTakesPage.jsx"
      provides: "Keyboard-accessible group selection cards"
      contains: "role=\"button\""
    - path: "src/pages/VisionTab.jsx"
      provides: "Keyboard-accessible category headers"
      contains: "role=\"button\""
    - path: "src/pages/StudyTogetherPage.jsx"
      provides: "Keyboard-accessible book cards"
      contains: "role=\"button\""
    - path: "src/pages/ResultsPage.jsx"
      provides: "Keyboard-accessible reveal cards"
      contains: "role=\"button\""
  key_links:
    - from: "all 5 page files"
      to: "keyboard events"
      via: "onKeyDown handler on interactive elements"
      pattern: "onKeyDown.*Enter|Space"
---

<objective>
Add button semantics and keyboard handlers to interactive cards across 5 pages so keyboard and assistive technology users can navigate and activate them.

Purpose: Addresses A11Y-01 — interactive cards that currently use `motion.div` or `div` with `onClick` are invisible to screen readers and unreachable by keyboard. Adding `role="button"`, `tabIndex={0}`, and `onKeyDown` for Enter/Space makes them operable.

Output: 5 updated page files with accessible interactive cards.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add button semantics to VaultPage, HotTakesPage, and ResultsPage cards</name>
  <files>src/pages/VaultPage.jsx, src/pages/HotTakesPage.jsx, src/pages/ResultsPage.jsx</files>
  <action>
For each interactive card element that uses `onClick` on a non-button element (`motion.div` or `div`), add three attributes:

1. `role="button"` — announces as button to screen readers
2. `tabIndex={0}` — makes it focusable via Tab key
3. `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); /* existing onClick logic */ } }}` — keyboard activation

**VaultPage.jsx** (5 cards around lines 153-340):
- Multiple Choice card (`motion.div` with `onClick={() => navigate(...)}`  at ~line 159)
- Deep Dive card (~line 195)
- Predict Your Partner card (~line 231)
- Finish My Sentence card (~line 267)
- Hot Takes card (~line 303)
Each card: add `role="button"`, `tabIndex={0}`, and `onKeyDown` that calls the same `navigate()` on Enter/Space. Also add `aria-label` with the card title text (e.g., `aria-label="Multiple Choice — pick the same answers and see how well you match"`).

**HotTakesPage.jsx** (group cards around line 333):
- `motion.div` with `onClick={() => handleSelectGroup(group.id)}` at ~line 343
Add `role="button"`, `tabIndex={0}`, and `onKeyDown` calling `handleSelectGroup(group.id)` on Enter/Space.

**ResultsPage.jsx** (reveal cards around line 280):
- `motion.div` with `onClick={() => toggleReveal(q.id)}` at ~line 287
Add `role="button"`, `tabIndex={0}`, and `onKeyDown` calling `toggleReveal(q.id)` on Enter/Space. Add `aria-label` that includes "tap to reveal" or "revealed" state.

For the `onKeyDown` handler, use `e.preventDefault()` before calling the action to prevent Space from scrolling the page.
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && npx -y eslint --no-eslintrc --rule '{"no-undef":"error"}' --parser-options=ecmaVersion:2022,ecmaFeatures:{jsx:true},sourceType:module src/pages/VaultPage.jsx src/pages/HotTakesPage.jsx src/pages/ResultsPage.jsx 2>/dev/null || npm run build 2>&1 | head -30</automated>
  </verify>
  <done>All interactive cards in VaultPage (5), HotTakesPage (group cards), and ResultsPage (reveal cards) have role="button", tabIndex={0}, and onKeyDown handlers for Enter/Space activation</done>
</task>

<task type="auto">
  <name>Task 2: Add button semantics to VisionTab category headers and StudyTogetherPage book cards</name>
  <files>src/pages/VisionTab.jsx, src/pages/StudyTogetherPage.jsx</files>
  <action>
**VisionTab.jsx** (category headers around line 480):
- `div` with `onClick={() => { setExpandedCat(...) }}` at ~line 481
This is a category accordion header. Add `role="button"`, `tabIndex={0}`, `aria-expanded={isExpanded}`, and `onKeyDown` calling the same toggle logic on Enter/Space.

**StudyTogetherPage.jsx** (BookCard component around line 470-490):
- The outer `div` at ~line 488 with `onClick={() => { if (!isExpanded) onToggle() }}`
- The header `div` at ~line 492 with `onClick` for collapse when expanded
For the outer card: add `role="button"`, `tabIndex={0}`, `aria-expanded={isExpanded}`, and `onKeyDown` calling `onToggle()` on Enter/Space (matching the existing click logic — only expand when not expanded).
For the inner header: since the outer div already handles keyboard for expand, the inner header click (collapse) can keep its existing onClick without separate keyboard handling — the outer div's keyboard handler should handle both expand and collapse. Simplify: make the outer div's `onClick` and `onKeyDown` always call `onToggle()` (not conditionally), and remove the inner header's separate onClick. This makes the accordion pattern standard: click anywhere on the card toggles.

Actually, to keep the existing behavior minimal and avoid regressions: keep both onClick handlers as-is, but add `role="button"`, `tabIndex={0}`, `aria-expanded={isExpanded}`, and `onKeyDown` to the outer card that calls `onToggle()` on Enter/Space regardless of state (toggling is the expected keyboard behavior for an accordion).
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>VisionTab category headers and StudyTogetherPage book cards have role="button", tabIndex={0}, aria-expanded, and onKeyDown handlers for Enter/Space activation</done>
</task>

</tasks>

<verification>
Run `npm run build` — must succeed with no errors. Then manually verify in browser: Tab through VaultPage cards, press Enter to navigate; Tab through HotTakesPage group cards; Tab through ResultsPage reveal cards and press Space to toggle.
</verification>

<success_criteria>
- All 5 pages' interactive cards have `role="button"`, `tabIndex={0}`, and `onKeyDown` handlers
- Build succeeds with no errors
- Keyboard Tab reaches every interactive card, Enter/Space activates it
</success_criteria>

<output>
After completion, create `.planning/phases/07-accessibility/07-01-SUMMARY.md`
</output>
