---
phase: 14-capacitor-install-platform-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - capacitor.config.json
  - vite.config.js
  - .gitignore
  - ios/
  - android/
  - android/app/build.gradle
  - ios/App/App/Info.plist
  - tests/native-config.test.js
  - tests/platforms-exist.test.js
autonomous: false
requirements: [NATIVE-01, NATIVE-02, NATIVE-03, NATIVE-04]

must_haves:
  truths:
    - "Capacitor 8.3.4 packages are installed and listed in package.json dependencies"
    - "capacitor.config.json exists at repo root with appId com.theusquiz.app, appName 'The Us Quiz', webDir 'dist', and NO server.url"
    - "vite.config.js sets base: './' so asset paths resolve under capacitor:// and https://localhost"
    - "ios/App/App.xcodeproj exists and Info.plist contains NSCameraUsageDescription + NSPhotoLibraryUsageDescription + NSPhotoLibraryAddUsageDescription with paper-themed copy"
    - "android/app/build.gradle has signingConfigs.release wired to keystore.properties"
    - "android/keystore.properties, *.jks, *.keystore, sync output dirs, and build outputs are gitignored"
    - "Native iOS and Android project skeletons are committed to git (per research A6)"
    - "npx cap sync runs without error after npm run build"
    - "Installed Android build (emulator or USB device) loads /assets/*.js from the bundle — Chrome DevTools Network tab shows zero requests to theusquiz.com for HTML/JS/CSS"
  artifacts:
    - path: "capacitor.config.json"
      provides: "Capacitor app config (appId, appName, webDir, plugins, ios/android blocks)"
      contains: "com.theusquiz.app"
    - path: "vite.config.js"
      provides: "Vite build config with base: './'"
      contains: "base: './'"
    - path: "ios/App/App.xcodeproj/project.pbxproj"
      provides: "iOS Xcode project scaffolded by npx cap add ios"
    - path: "ios/App/App/Info.plist"
      provides: "iOS Info.plist with camera + photo library permission strings (Phase 16 will activate)"
      contains: "NSCameraUsageDescription"
    - path: "android/app/build.gradle"
      provides: "Android module Gradle build with signingConfigs.release"
      contains: "signingConfigs"
    - path: ".gitignore"
      provides: "Excludes keystore + sync output + native build outputs"
      contains: "keystore.properties"
    - path: "tests/native-config.test.js"
      provides: "Vitest checks for capacitor.config.json shape and vite base path"
    - path: "tests/platforms-exist.test.js"
      provides: "Vitest checks for ios/ + android/ project files"
  key_links:
    - from: "capacitor.config.json"
      to: "dist/"
      via: "webDir property"
      pattern: "webDir.*dist"
    - from: "vite.config.js"
      to: "Capacitor WebView asset resolution"
      via: "base: './'"
      pattern: "base.*\\./"
    - from: "android/app/build.gradle"
      to: "android/keystore.properties"
      via: "signingConfigs.release block reading Properties file"
      pattern: "keystore\\.properties"
---

<objective>
Install Capacitor 8.3.4, scaffold iOS + Android native projects, switch Vite to relative base path, and set up the Android signing pipeline — so a single `npm run build && npx cap sync` produces native projects that load from bundled assets (no remote URL).

Purpose: This is the foundation of the v2.0 native wrap. Bundled-asset loading (NATIVE-04) is the core mitigation for Apple Section 4.2 "thin wrapper" rejection. The iOS Xcode project + Android Gradle project produced here are committed so Codemagic (Plan 02) and Android Studio can build directly from a clean checkout.
Output: capacitor.config.json, updated vite.config.js, ios/ skeleton with seeded Info.plist permission strings, android/ skeleton with signingConfigs.release wired to a gitignored keystore.properties, updated .gitignore, and two Wave-0 vitest config-shape tests.

**Platform note:** This work happens on John's Windows box. iOS-specific validation (simulator launch, Xcode build) is deferred to Plan 02's Codemagic pipeline since there is no Mac available. iOS validation in this plan is limited to "the Xcode project files exist and Info.plist parses" — not "the app boots on iOS." Android validation (emulator or USB device) is the load-bearing NATIVE-04 evidence for this plan.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md

<interfaces>
Existing vite.config.js — base property is the only new key. Keep server, build.rollupOptions.output.manualChunks, and test blocks unchanged. Insert `base: './',` as the first key after the opening defineConfig brace.

Target capacitor.config.json contents (full file):

    {
      "appId": "com.theusquiz.app",
      "appName": "The Us Quiz",
      "webDir": "dist",
      "android": {
        "adjustMarginsForEdgeToEdge": "auto"
      },
      "ios": {
        "contentInset": "always"
      },
      "plugins": {
        "StatusBar": {
          "style": "DARK",
          "backgroundColor": "#FFF8F0",
          "overlaysWebView": false
        }
      }
    }

Critical: no `server` block. No `server.url`. No `server.androidScheme` override (default https is correct).

iOS Info.plist permission keys to seed now (Phase 16 activates them):
- NSCameraUsageDescription = "The Us Quiz uses your camera so you can take photos for your Daily Photo Challenge prompts."
- NSPhotoLibraryUsageDescription = "The Us Quiz needs photo library access so you can pick existing photos to answer Daily Photo prompts."
- NSPhotoLibraryAddUsageDescription = "The Us Quiz saves photos you take to your photo library."

Android signingConfigs.release snippet — insert into android/app/build.gradle per research Pattern 6 (research section "Pattern 6: Android keystore generation"). Top of file gets the `def keystorePropertiesFile = rootProject.file("keystore.properties")` block; inside `android { }` add `signingConfigs { release { ... } }` and `buildTypes { release { signingConfig signingConfigs.release; minifyEnabled false } }`. Wrap key reads in `if (keystorePropertiesFile.exists())` so CI without the file does not fail at Gradle config time.

.gitignore additions (append, do not replace existing rules):
- Sync output: ios/App/App/public/, android/app/src/main/assets/public/
- Native build outputs: ios/App/build/, ios/App/Pods/, ios/App/Podfile.lock, android/app/build/, android/.gradle/, android/build/, android/local.properties
- Signing secrets: android/keystore.properties, *.jks, *.keystore, *.p8, *.p12, *.mobileprovision
- Build artifacts: *.ipa, *.apk, *.aab

Decision: COMMIT the native project skeletons (ios/, android/) to git — research Pattern A6. They contain manually configured plist entries and Gradle signing config. GITIGNORE the sync output and build artifacts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Capacitor packages, write capacitor.config.json, switch Vite to relative base, add Wave-0 config tests</name>
  <files>package.json, package-lock.json, capacitor.config.json, vite.config.js, tests/native-config.test.js</files>
  <read_first>
    - vite.config.js (current shape — only add base: './', keep all other keys)
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md (Pattern 1, Pattern 2)
    - package.json (verify Node engines + existing test script)
  </read_first>
  <action>
Atomic commit boundary. Do every step in this task, then commit as one.

1. Install Capacitor packages (exact versions per research; pin with caret):

    npm i @capacitor/core@^8.3.4
    npm i -D @capacitor/cli@^8.3.4
    npm i @capacitor/ios@^8.3.4 @capacitor/android@^8.3.4
    npm i @capacitor/status-bar@^8.0.2
    npm i @capacitor/splash-screen@^8.0.1
    npm i @capacitor/app@^8.1.0

`@capacitor/splash-screen` is installed now per research Open Question 3 (plugin in place, artwork deferred to Phase 18). `@capacitor/app` is installed now per research Open Question 4 (Phase 15 needs it; install is free in this phase). DO NOT install `@capacitor-community/safe-area` — modern WebView reports `env(safe-area-inset-*)` natively (research Standard Stack alternatives).

2. Create capacitor.config.json at repo root with the exact JSON shown in the <interfaces> block above. The StatusBar plugin block is config-only here — the actual `StatusBar.setStyle()` JS call lives in Plan 02.

3. Modify vite.config.js — add exactly one line: `base: './',` directly after the opening `defineConfig({` brace, before `plugins: [react()]`. Do NOT change any other key. The relative base is required so `/assets/index.abc.js` resolves under `capacitor://localhost/` (iOS) and `https://localhost/` (Android) — research Pitfall 2.

4. Create tests/native-config.test.js with these assertions (uses existing vitest setup):

    import { describe, it, expect } from 'vitest'
    import fs from 'node:fs'
    import path from 'node:path'

    describe('capacitor.config.json', () => {
      const cfg = JSON.parse(fs.readFileSync(path.resolve('capacitor.config.json'), 'utf8'))
      it('has correct appId', () => { expect(cfg.appId).toBe('com.theusquiz.app') })
      it('has correct appName', () => { expect(cfg.appName).toBe('The Us Quiz') })
      it('webDir points to dist', () => { expect(cfg.webDir).toBe('dist') })
      it('does NOT set server.url (NATIVE-04)', () => { expect(cfg.server?.url).toBeUndefined() })
      it('configures StatusBar plugin with paper color', () => {
        expect(cfg.plugins?.StatusBar?.style).toBe('DARK')
        expect(cfg.plugins?.StatusBar?.backgroundColor).toBe('#FFF8F0')
      })
      it('configures Android edge-to-edge handling', () => {
        expect(cfg.android?.adjustMarginsForEdgeToEdge).toBe('auto')
      })
    })

    describe('vite.config.js', () => {
      const src = fs.readFileSync(path.resolve('vite.config.js'), 'utf8')
      it("sets base: './' for Capacitor scheme compatibility", () => {
        expect(src).toMatch(/base:\s*['"]\.\/['"]/)
      })
    })

5. Run `npm test` to confirm the new test file passes and no existing test broke.

Deviation handling: If `npm view @capacitor/core version` at execution time returns 9.x or newer, STOP and check 14-RESEARCH.md A7. The plan targets 8.x; a 9.0 release within the research window is flagged in Open Questions. Do not silently install 9.x — pause for John's input.
  </action>
  <verify>
    <automated>npm test -- --run native-config</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include @capacitor/core, @capacitor/ios, @capacitor/android, @capacitor/status-bar, @capacitor/splash-screen, @capacitor/app at ^8.x
    - package.json devDependencies include @capacitor/cli at ^8.x
    - capacitor.config.json exists at repo root
    - capacitor.config.json has appId "com.theusquiz.app", appName "The Us Quiz", webDir "dist"
    - capacitor.config.json does NOT contain a "server" key
    - capacitor.config.json has plugins.StatusBar.backgroundColor "#FFF8F0"
    - capacitor.config.json has android.adjustMarginsForEdgeToEdge "auto"
    - vite.config.js contains `base: './'` exactly (relative path, single quotes)
    - tests/native-config.test.js exists and `npm test` passes
    - No regression: existing tests still pass
  </acceptance_criteria>
  <done>Capacitor 8.x is installed, capacitor.config.json is the bundled-asset shape from research Pattern 1, vite.config.js has relative base path, and Wave-0 config tests assert all of the above.</done>
</task>

<task type="auto">
  <name>Task 2: Add iOS + Android platforms with `npx cap add` and seed Info.plist permission strings</name>
  <files>ios/, android/, ios/App/App/Info.plist, tests/platforms-exist.test.js</files>
  <read_first>
    - capacitor.config.json (just created in Task 1 — npx cap add reads this)
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md (Pattern 1, "Decision: commit native projects" note, Info.plist additions section)
  </read_first>
  <action>
Atomic commit boundary. This is one task because `npx cap add ios` and `npx cap add android` are tightly coupled to the same config and produce parallel skeletons.

1. Run a clean web build first so dist/ exists (required by sync even though `add` itself does not copy):

    npm run build

2. Add iOS platform:

    npx cap add ios

This generates ios/App/App.xcodeproj, ios/App/App/Info.plist, ios/App/Podfile, and friends. The command will NOT run `pod install` — Codemagic handles it in Plan 02.

3. Seed ios/App/App/Info.plist with Phase 16's camera + photo library permission strings so the keys exist now (Phase 16 just turns them on). Inside the top-level <dict>, add the three keys with the exact paper-themed copy from the <interfaces> block. These strings are user-facing in the iOS permission dialogs — keep voice consistent with the rest of the app (warm, plain language; no marketing jargon).

4. Add Android platform:

    npx cap add android

This generates android/app/build.gradle, android/app/src/main/AndroidManifest.xml, the Gradle wrapper, etc.

5. Verify AndroidManifest.xml is minimal — Capacitor 8 scaffolds with only INTERNET permission by default. Phase 16 (camera) and Phase 17 (push) add more. Do not edit it in this plan beyond what Capacitor generated.

6. Run `npx cap sync` to copy dist/ into both native projects and confirm sync is clean. Expected output mentions copying web assets from dist into ios/App/App/public and android/app/src/main/assets/public. Any error here is load-bearing — stop and diagnose before committing.

7. Create tests/platforms-exist.test.js with filesystem assertions:

    import { describe, it, expect } from 'vitest'
    import fs from 'node:fs'
    import path from 'node:path'

    describe('iOS platform scaffolded', () => {
      it('Xcode project exists', () => {
        expect(fs.existsSync(path.resolve('ios/App/App.xcodeproj/project.pbxproj'))).toBe(true)
      })
      it('Info.plist contains camera usage description', () => {
        const plist = fs.readFileSync(path.resolve('ios/App/App/Info.plist'), 'utf8')
        expect(plist).toContain('NSCameraUsageDescription')
        expect(plist).toContain('Daily Photo Challenge')
      })
      it('Info.plist contains photo library usage descriptions', () => {
        const plist = fs.readFileSync(path.resolve('ios/App/App/Info.plist'), 'utf8')
        expect(plist).toContain('NSPhotoLibraryUsageDescription')
        expect(plist).toContain('NSPhotoLibraryAddUsageDescription')
      })
    })

    describe('Android platform scaffolded', () => {
      it('module build.gradle exists', () => {
        expect(fs.existsSync(path.resolve('android/app/build.gradle'))).toBe(true)
      })
      it('AndroidManifest.xml exists', () => {
        expect(fs.existsSync(path.resolve('android/app/src/main/AndroidManifest.xml'))).toBe(true)
      })
    })

Deviation handling:
- If `npx cap add ios` fails on Windows because of a missing macOS-only dependency (it should not — `cap add` is pure Node + file copy), capture the exact error and stop. The research is explicit that `cap add` runs on Windows; only `cap open ios` + Xcode build need macOS.
- If `npx cap sync` fails with "no such directory: dist", confirm `npm run build` completed and dist/ was produced. Do not commit empty native projects without a clean sync.
  </action>
  <verify>
    <automated>npm test -- --run platforms-exist</automated>
  </verify>
  <acceptance_criteria>
    - ios/App/App.xcodeproj/project.pbxproj exists
    - ios/App/App/Info.plist exists and contains NSCameraUsageDescription, NSPhotoLibraryUsageDescription, NSPhotoLibraryAddUsageDescription
    - Info.plist strings contain the substring "Daily Photo Challenge" (paper-themed copy verified)
    - android/app/build.gradle exists
    - android/app/src/main/AndroidManifest.xml exists
    - `npx cap sync` completes without error (manual confirmation captured in task summary)
    - tests/platforms-exist.test.js passes
  </acceptance_criteria>
  <done>iOS Xcode project and Android Gradle project are scaffolded by Capacitor, Info.plist has Phase 16 permission strings pre-seeded with paper-themed copy, and `npx cap sync` runs cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Generate Android keystore, wire signingConfigs.release in build.gradle, and finalize .gitignore</name>
  <files>android/app/build.gradle, .gitignore (android/keystore.properties and android/release.keystore created locally — gitignored, NOT committed)</files>
  <read_first>
    - android/app/build.gradle (Capacitor-generated default — find the `android { ... }` block boundaries before editing)
    - .gitignore (current contents — append, do not replace)
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md (Pattern 6 — keytool command + Gradle snippet)
  </read_first>
  <action>
Atomic commit boundary. This task produces two committed file changes (android/app/build.gradle, .gitignore) and two local-only files that MUST be gitignored before any `git add`.

1. Append the gitignore block FIRST so any keystore artifacts created in step 2 cannot accidentally be committed. Append the exact block listed in the <interfaces> ".gitignore additions" section to the end of .gitignore. Confirm the existing .gitignore is not overwritten — append only.

2. Generate the Android release keystore on John's Windows box. Run from repo root in PowerShell:

    keytool -genkeypair -v `
      -keystore android/release.keystore `
      -storetype PKCS12 `
      -alias theusquiz `
      -keyalg RSA `
      -keysize 2048 `
      -validity 10000

`keytool` ships with Android Studio's bundled JDK (research Environment Availability table). Prompted values: keystore password (record in 1Password/Bitwarden), key password (use the SAME as keystore password per research recommendation), name "John Allen", org/OU "The Us Quiz", real city/state/country.

After generation, android/release.keystore exists locally. It MUST be gitignored (verified in step 1). It MUST be backed up to `C:\My Vaults\CommandCenter\reports\secrets\` plus the password manager as an encrypted attachment — research Pitfall 4. If this backup is skipped and the file is lost later, Google Play rejects every future update permanently.

3. Create android/keystore.properties (also gitignored, also local-only):

    storeFile=../release.keystore
    storePassword=<the password you just set>
    keyAlias=theusquiz
    keyPassword=<same as storePassword>

Path is relative to android/app/build.gradle consuming it — `../release.keystore` resolves to android/release.keystore.

4. Modify android/app/build.gradle — add the signingConfigs.release block per research Pattern 6. Insert the `def keystorePropertiesFile = rootProject.file("keystore.properties")` plus Properties load BEFORE the existing `android { ... }` block. Insert `signingConfigs { release { ... } }` and the matching `buildTypes { release { signingConfig signingConfigs.release; minifyEnabled false } }` INSIDE the `android { }` block. Use the exact code from the research Pattern 6 snippet.

If Capacitor 8 already scaffolded a `buildTypes { release { ... } }` block, MERGE — add `signingConfig signingConfigs.release` to it, do not duplicate.

5. Confirm the wiring locally: from android/, run `./gradlew tasks` (or open Android Studio and let it sync). If Gradle reports "Could not get unknown property 'keystoreProperties'", the `keystoreProperties.load` is misplaced — it must run at the top of the file, outside the `android {}` block.

Deviation handling:
- Research recommends one keystore password reused as the key password. If John wants separate passwords later, the Gradle snippet already supports it via the two distinct Properties keys — no plan change needed.
- If `keytool` is not on PATH on John's Windows box, install Android Studio first (research lists it as the JDK source). Do not chase a standalone JDK.
- Google Play App Signing (research Pitfall 4 mitigation #3) is set up in Phase 19 when the Play Console listing exists. Do not configure it in this plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const gi=fs.readFileSync('.gitignore','utf8');const bg=fs.readFileSync('android/app/build.gradle','utf8');const okGi=gi.includes('keystore.properties')&&gi.includes('*.keystore')&&gi.includes('*.apk');const okBg=bg.includes('signingConfigs')&&bg.includes('keystore.properties');if(!okGi||!okBg){console.error('FAIL gi='+okGi+' bg='+okBg);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains "android/keystore.properties", "*.keystore", "*.jks", "*.ipa", "*.apk", "*.aab"
    - .gitignore contains "ios/App/App/public/" and "android/app/src/main/assets/public/" (sync output)
    - android/app/build.gradle contains a "signingConfigs" block with a "release" entry
    - android/app/build.gradle references "keystore.properties" via rootProject.file(...)
    - android/release.keystore and android/keystore.properties exist locally but are NOT tracked by git (verify with `git status --ignored` or `git check-ignore android/release.keystore`)
    - Android Studio (or `./gradlew tasks` from android/) syncs without "unknown property" errors
    - No regression: existing tests still pass
  </acceptance_criteria>
  <done>Android keystore is generated and backed up, keystore.properties + build.gradle wire signing for release builds, and .gitignore prevents any signing material or build artifact from entering the repo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Install on Android emulator (or USB device) and confirm bundled-asset load via Chrome DevTools</name>
  <files>ios/, android/ (no file edits — runtime verification of NATIVE-04)</files>
  <action>
The executor's prep work for the user: run `npm run build && npx cap sync && npx cap open android` so Android Studio opens with the latest synced bundle. If a USB-connected Android device is available, ensure USB debugging is enabled. Otherwise spin up an emulator (Pixel 6 API 34+ recommended — Android 14 / 15).

Reason this is a checkpoint: the load-bearing NATIVE-04 evidence is "the installed app loads /assets/*.js from the bundle, not from theusquiz.com." This is a runtime claim — only a real install + DevTools Network capture can prove it. The vitest config-shape checks in Tasks 1 + 2 prove the config is correct, but they do not prove the runtime behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('android/app/src/main/assets/public/index.html'))throw new Error('sync output missing — run npx cap sync');console.log('sync output present, ready for install')"</automated>
  </verify>
  <what-built>Android emulator/device install of the wrapped app, with web assets bundled from dist/ into android/app/src/main/assets/public/ and loaded under https://localhost/ inside the WebView.</what-built>
  <how-to-verify>
    1. In Android Studio: Run → Run 'app' (target = emulator or USB device).
    2. Wait for the app to install and launch. Expect the existing AuthPage to render.
    3. On your development machine, open Chrome → chrome://inspect/#devices → find "The Us Quiz" in the WebView list → click "inspect".
    4. In the DevTools that opens, switch to the Network tab → tick "Preserve log" → reload the WebView inside the app (Ctrl+R in DevTools).
    5. Inspect the Network table:
       - Confirm requests for "/assets/index-*.js", "/assets/index-*.css", and "index.html" load from "https://localhost/..." (Android default scheme).
       - Confirm ZERO requests go to "theusquiz.com" or "*.vercel.app" for HTML/JS/CSS.
       - It IS expected that requests TO Supabase (https://<project>.supabase.co/...) still appear — that is API/realtime traffic, not asset traffic. Sign-in or visit a quiz to trigger them so you can distinguish them.
    6. (Optional sanity check) Toggle airplane mode on the emulator/device, force-quit the app, relaunch. The web bundle should boot to AuthPage even with no network. Supabase calls will fail (expected) but the React app itself loads — that is the Apple Section 4.2 mitigation behavior.
    7. Confirm the app is signed in debug mode (Android Studio "Run" uses debug signing, not release). Release-signing validation is deferred to Phase 19 when a release APK is generated; for this plan, debug install is sufficient to prove NATIVE-04.
  </how-to-verify>
  <done>User has confirmed via Chrome DevTools Network tab that the installed Android app loads HTML/JS/CSS from the bundle (https://localhost/) and makes NO asset requests to theusquiz.com.</done>
  <resume-signal>Type "approved" with a one-line note about which device/emulator was used, or describe issues (white screen, 404s, unexpected network calls).</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes including tests/native-config.test.js and tests/platforms-exist.test.js
- capacitor.config.json contains appId/appName/webDir and lacks any `server` block
- ios/App/App/Info.plist contains the three permission keys with paper-themed copy
- android/app/build.gradle has signingConfigs.release wired to keystore.properties
- `git check-ignore android/release.keystore` returns true (file ignored)
- Chrome DevTools confirms zero theusquiz.com asset requests during runtime (Task 4 checkpoint)
</verification>

<success_criteria>
Maps to ROADMAP.md Phase 14 Success Criteria 1, 2, 3 (per the table at the bottom of this plan).

- `npm run build && npx cap sync` completes without manual file copying (SC1, partial — SC1 is fully met when combined with Plan 02's BUILDING.md and Codemagic wiring)
- Installed Android app loads from bundled assets — verified via Chrome DevTools Network capture (SC3)
- iOS Xcode project is scaffolded with Info.plist seeded for Phase 16 — SC2 is fully verified in Plan 02 once Codemagic produces a TestFlight build
</success_criteria>

<output>
After completion, create `.planning/phases/14-capacitor-install-platform-setup/14-01-SUMMARY.md`
</output>

---

## Success Criteria Mapping

| ROADMAP Success Criterion | Tasks |
|---------------------------|-------|
| SC1: `npm run build && npx cap sync` followed by one command opens a working app in Xcode and in Android Studio | Task 1 (cap install + base path), Task 2 (cap add ios + cap add android + cap sync), Task 3 (signing wired so Android Studio can actually build) — full SC1 closure also depends on Plan 02 BUILDING.md `npm run cap:android` / `npm run cap:ios` scripts |
| SC2: Installed iOS app loads from bundled assets (no network for HTML/JS/CSS) | Task 1 (no server.url in config, base './' in vite), Task 2 (ios project scaffolded with seeded Info.plist) — runtime evidence on iOS is captured in Plan 02 Codemagic TestFlight build |
| SC3: Installed Android app loads from bundled assets | Task 1, Task 2, Task 3, Task 4 (Chrome DevTools Network capture is the load-bearing evidence) |
| SC4: Safe-area + status-bar handling on notched devices | Plan 02 |
| SC5: BUILDING.md lets a developer reproduce both builds | Plan 02 |
