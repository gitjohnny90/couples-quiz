---
phase: 14-capacitor-install-platform-setup
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - index.html
  - src/index.css
  - src/main.jsx
  - codemagic.yaml
  - BUILDING.md
  - package.json
  - tests/viewport-meta.test.js
  - tests/safe-area-css.test.js
  - tests/build-docs.test.js
autonomous: false
requirements: [NATIVE-08, NCAP-07]

must_haves:
  truths:
    - "index.html viewport meta tag includes viewport-fit=cover"
    - "src/index.css applies env(safe-area-inset-top) to the .page container and html/body has --bg-paper background"
    - "src/main.jsx calls StatusBar.setStyle(Style.Dark) + setBackgroundColor #FFF8F0 + setOverlaysWebView(false), gated on Capacitor.isNativePlatform() so web is untouched"
    - "Android edge-to-edge handling works via capacitor.config.json adjustMarginsForEdgeToEdge: 'auto' (Capacitor 8 built-in, no MainActivity.java edits)"
    - "codemagic.yaml exists at repo root and defines an ios-capacitor-workflow that builds the IPA and submits to TestFlight"
    - "BUILDING.md exists at repo root with Prerequisites, Android local build, iOS via Codemagic, and Troubleshooting sections"
    - "package.json scripts include cap:sync, cap:android, cap:ios"
    - "On a notched iPhone (Codemagic TestFlight build artifact) the warm paper bg extends into the status bar and BottomNav stays above the home indicator"
    - "On an Android emulator (API 34+), safe-area-inset-bottom keeps BottomNav above the gesture bar"
  artifacts:
    - path: "index.html"
      provides: "Viewport meta with viewport-fit=cover"
      contains: "viewport-fit=cover"
    - path: "src/index.css"
      provides: "html/body paper background + .page safe-area-inset-top padding"
      contains: "env(safe-area-inset-top"
    - path: "src/main.jsx"
      provides: "Native-only StatusBar initialization gated on Capacitor.isNativePlatform()"
      contains: "Capacitor.isNativePlatform"
    - path: "codemagic.yaml"
      provides: "Codemagic iOS Capacitor build + TestFlight submission workflow"
      contains: "ios-capacitor-workflow"
    - path: "BUILDING.md"
      provides: "Build documentation for Android (local) + iOS (Codemagic)"
      contains: "Android local build"
    - path: "tests/viewport-meta.test.js"
      provides: "Asserts index.html viewport includes viewport-fit=cover"
    - path: "tests/safe-area-css.test.js"
      provides: "Asserts src/index.css uses env(safe-area-inset-*) on .page and .bottom-nav"
    - path: "tests/build-docs.test.js"
      provides: "Asserts BUILDING.md exists with required section headings"
  key_links:
    - from: "src/main.jsx"
      to: "@capacitor/status-bar"
      via: "import { StatusBar, Style } and Capacitor.isNativePlatform() gate"
      pattern: "@capacitor/status-bar"
    - from: "src/index.css"
      to: "Notched device chrome"
      via: "env(safe-area-inset-top) on .page"
      pattern: "env\\(safe-area-inset-top"
    - from: "codemagic.yaml"
      to: "App Store Connect API"
      via: "integrations.app_store_connect: theusquiz-ascapi"
      pattern: "app_store_connect"
---

<objective>
Wire the visual + build-pipeline finish work for Phase 14: paper-themed status bar, safe-area-inset handling on `.page` and `.bottom-nav`, gated native StatusBar plugin initialization, Codemagic iOS build pipeline targeting TestFlight, BUILDING.md for both platforms, and npm convenience scripts.

Purpose: Plan 01 produced a buildable Android target with bundled assets. Plan 02 closes the visual gap (status bar matches the warm paper aesthetic, no UI clipping on notched devices) and the operational gap (a developer with a clean checkout can produce an Android APK locally on Windows and an iOS IPA via cloud CI — neither requires a Mac sitting in front of John).
Output: Modified `index.html` + `src/index.css` + `src/main.jsx`, new `codemagic.yaml`, new `BUILDING.md`, three new vitest checks for viewport / safe-area / docs, npm script additions.

**Platform note:** This work happens on John's Windows box. iOS validation (Codemagic build → TestFlight install → notched-device visual check) is the load-bearing evidence for NCAP-07's iOS half — but it depends on operational prerequisites that may not be in place at plan start (see Prerequisites section below). Plan 02 produces the codemagic.yaml and docs unconditionally; the actual Codemagic build run + TestFlight install is the Task 5 checkpoint and is gated on those prerequisites.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<prerequisites>
## Wave 0 operational prerequisites (NOT executor tasks — John-only setup)

These are blockers for the Codemagic + TestFlight half of this plan. Plan 14-01 can ship in full with NONE of these in place (Android-only). Plan 14-02's Tasks 1–4 (viewport, CSS, StatusBar JS, npm scripts, codemagic.yaml file, BUILDING.md file) can also ship without these. Only Task 5 (checkpoint: Codemagic build → TestFlight install on a notched iPhone) requires them.

1. **Apple Developer Program enrollment** ($99/yr). 24–48h approval lag after John submits the application — possibly longer if Apple flags identity verification. Without this, no Apple Developer Team ID exists, no bundle identifier can be reserved, and Codemagic cannot fetch signing files.

2. **Bundle identifier reservation** in App Store Connect: `com.theusquiz.app` registered to John's team (research A1). Requires step 1 complete.

3. **App Store Connect API key** (research Pattern 7): App Store Connect → Users & Access → Integrations → App Store Connect API → create key with "App Manager" role → download the `.p8` file once (it cannot be re-downloaded — guard it).

4. **Codemagic account** created and linked to the GitHub repo. Free tier (500 macOS M2 min/mo) is sufficient.

5. **Codemagic Apple Developer Portal integration** named `theusquiz-ascapi`: in the Codemagic UI under Teams → Integrations → Apple Developer Portal → upload the `.p8`, issuer ID, and key ID from step 3.

**If items 1–5 are not yet complete when Plan 02 starts:** the executor can still ship Tasks 1–4 (they have no operational dependency). Task 5 is held until John confirms all prerequisites. The Android visual check inside Task 5 (Android emulator safe-area validation) is INDEPENDENT of the iOS prerequisites and can run on John's Windows box anytime.

**Recommendation:** Start the Apple Developer Program enrollment in parallel with Plan 01 execution so the approval lag overlaps with build work — typical approval is back by the time Plan 02 reaches Task 5.
</prerequisites>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md
@.planning/phases/14-capacitor-install-platform-setup/14-01-SUMMARY.md

<interfaces>
Existing index.html viewport meta (from src read at plan time):

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

Target — append `, viewport-fit=cover` (research Pattern 3):

    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />

Existing src/index.css landmarks:
- `.page { ... }` rule starts at line 162 (single-column layout, current padding).
- `.bottom-nav { padding-bottom: env(safe-area-inset-bottom, 0px); }` already exists at line 187 — DO NOT touch.
- No existing `html, body { background-color: ... }` rule for the paper color — body has its ruled-lines gradient bg. Add an `html, body { background-color: var(--bg-paper); }` rule so paper color shows through the transparent status bar on Android 16+.

Existing App.jsx root markup (from grep at plan time):

    <div className="app" style={{ position: 'relative' }}>

Status bar JS init goes in src/main.jsx, not App.jsx, so it runs once at boot before React mounts.

Capacitor StatusBar plugin import + gate (research Pattern 5):

    import { Capacitor } from '@capacitor/core'
    import { StatusBar, Style } from '@capacitor/status-bar'

    async function configureNativeStatusBar() {
      if (!Capacitor.isNativePlatform()) return
      try {
        await StatusBar.setStyle({ style: Style.Dark })
        await StatusBar.setBackgroundColor({ color: '#FFF8F0' })
        await StatusBar.setOverlaysWebView({ overlay: false })
      } catch (err) {
        console.warn('StatusBar setup skipped:', err)
      }
    }
    configureNativeStatusBar()

`Style.Dark` = dark text/icons on light bg (research Pitfall 6 — naming is counterintuitive). The gate on `Capacitor.isNativePlatform()` keeps the web build (Vercel deployment) untouched — on web, StatusBar calls would either no-op or throw.

codemagic.yaml target structure (research Pattern 7 — copy verbatim):
- workflows.ios-capacitor-workflow.name = "iOS Capacitor Build"
- instance_type: mac_mini_m2 (free tier — research note)
- integrations.app_store_connect: theusquiz-ascapi
- ios_signing.distribution_type: app_store, bundle_identifier: com.theusquiz.app
- Scripts (in order): npm ci, npm run build, npx cap sync ios, cd ios/App && pod install && cd -, keychain initialize, app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create, keychain add-certificates, xcode-project use-profiles, xcode-project build-ipa --workspace "$XCODE_WORKSPACE" --scheme "$XCODE_SCHEME"
- artifacts: build/ios/ipa/*.ipa, /tmp/xcodebuild_logs/*.log
- publishing.app_store_connect.auth: integration, submit_to_testflight: true

Note on triggering: research Open Question 1 recommends START with manual trigger only (no auto-build on push) to conserve the 500-min free tier. Encode `triggering` as branch_patterns main BUT include an inline yaml comment noting John can comment out the `triggering` block at any time and trigger manually via Codemagic UI when monthly minutes are tight.

package.json scripts to add (research Pattern 8):

    "cap:sync": "npm run build && npx cap sync"
    "cap:android": "npm run cap:sync && npx cap open android"
    "cap:ios": "npm run cap:sync && npx cap open ios"

`cap:ios` works on Mac only; on Windows it errors with a clear message because `cap open ios` shells to `open -a Xcode`. Leave it in package.json anyway — useful when someone with a Mac contributes, and harmless on Windows because John never runs it.

BUILDING.md required headings (Wave-0 test asserts these substrings):
- `# Building The Us Quiz Native Apps`
- `## Prerequisites`
- `## Quick reference`
- `## Android local build`
- `## iOS build via Codemagic`
- `## Troubleshooting`

Full content per research "BUILDING.md outline" section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewport-fit=cover, html/body paper bg, and safe-area-inset-top on .page + viewport-meta Wave-0 test</name>
  <files>index.html, src/index.css, tests/viewport-meta.test.js, tests/safe-area-css.test.js</files>
  <read_first>
    - index.html (line 6 — viewport meta tag)
    - src/index.css (lines 162-200 — .page block + existing .bottom-nav safe-area rule)
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md (Pattern 3, Pattern 4)
  </read_first>
  <action>
Atomic commit boundary.

1. Modify index.html line 6 — append `, viewport-fit=cover` to the existing viewport content string. Final value:

    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />

Side effect for web: browsers ignore the option on non-notched devices, so this is safe to ship to Vercel too — research Pattern 3.

2. Modify src/index.css — add safe-area handling and html/body paper bg. Two changes:

   (a) Inside the existing `.page { ... }` rule (currently at line 162), add the safe-area padding additions. Use `calc(<existing padding> + env(safe-area-inset-top, 0px))` so the existing notebook-page padding is preserved on non-notched devices. The exact existing top padding value lives at line ~163 — read the file first and preserve it. Apply analogous additions for `padding-left` (60px margin-line + horizontal inset for landscape) and `padding-right` (horizontal inset).

   (b) Add a new rule at the top of the file (or anywhere not inside an existing block):

       html, body {
         background-color: var(--bg-paper);
       }

   This fills the area outside `.page` (status bar overlay zone on Android 16+ where the WebView extends edge-to-edge) with the paper color — research Pattern 4 explanation. The existing body rule with the ruled-lines gradient background still applies because `background` shorthand wasn't used.

3. Create tests/viewport-meta.test.js:

    import { describe, it, expect } from 'vitest'
    import fs from 'node:fs'
    import path from 'node:path'

    describe('index.html viewport meta', () => {
      const html = fs.readFileSync(path.resolve('index.html'), 'utf8')
      it('includes viewport-fit=cover for notched-device safe-area inset reporting', () => {
        expect(html).toMatch(/viewport-fit=cover/)
      })
      it('keeps width=device-width and initial-scale=1.0', () => {
        expect(html).toMatch(/width=device-width/)
        expect(html).toMatch(/initial-scale=1\.0/)
      })
    })

4. Create tests/safe-area-css.test.js:

    import { describe, it, expect } from 'vitest'
    import fs from 'node:fs'
    import path from 'node:path'

    describe('src/index.css safe-area handling', () => {
      const css = fs.readFileSync(path.resolve('src/index.css'), 'utf8')
      it('.bottom-nav uses env(safe-area-inset-bottom)', () => {
        // pre-existing — guard against regression
        expect(css).toMatch(/env\(safe-area-inset-bottom/)
      })
      it('.page (or html/body) uses env(safe-area-inset-top) for notched devices', () => {
        expect(css).toMatch(/env\(safe-area-inset-top/)
      })
      it('html or body sets paper background color via --bg-paper', () => {
        expect(css).toMatch(/html[^{]*\{[^}]*--bg-paper|body[^{]*\{[^}]*--bg-paper|html\s*,\s*body[^{]*\{[^}]*--bg-paper/)
      })
    })

5. Run `npm test` to confirm the two new test files pass and no existing test broke.

Deviation handling: If the existing `.page` rule already uses `calc(...)` for top padding, just add `+ env(safe-area-inset-top, 0px)` inside the existing calc. Do not nest calc()-in-calc().
  </action>
  <verify>
    <automated>npm test -- --run viewport-meta safe-area-css</automated>
  </verify>
  <acceptance_criteria>
    - index.html viewport meta contains "viewport-fit=cover"
    - src/index.css contains an html/body rule setting `background-color: var(--bg-paper)`
    - src/index.css `.page` rule includes `env(safe-area-inset-top` (preserved inside existing padding calc)
    - src/index.css `.bottom-nav` rule still contains `env(safe-area-inset-bottom` (regression guard)
    - tests/viewport-meta.test.js and tests/safe-area-css.test.js both exist and pass
    - `npm run build` still succeeds — Vercel deploy is unaffected by these CSS additions
  </acceptance_criteria>
  <done>Viewport meta declares cover layout, .page respects top safe-area inset, html/body fills the status bar area with paper color, and Wave-0 tests guard both.</done>
</task>

<task type="auto">
  <name>Task 2: Wire @capacitor/status-bar in src/main.jsx with isNativePlatform() gate + add cap:sync / cap:android / cap:ios npm scripts</name>
  <files>src/main.jsx, package.json</files>
  <read_first>
    - src/main.jsx (current contents — preserve existing React.StrictMode + createRoot wiring; insert StatusBar init BEFORE the render call so it runs at boot)
    - package.json (existing scripts block — append new scripts, do not replace)
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md (Pattern 5, Pattern 8, Pitfall 6)
  </read_first>
  <action>
Atomic commit boundary.

1. Modify src/main.jsx — add the StatusBar init block at the top of the file, after the existing imports, BEFORE the `createRoot(...).render(...)` call. Use the exact code from the <interfaces> `Capacitor StatusBar plugin import + gate` snippet (research Pattern 5).

Key points:
- Gate on `Capacitor.isNativePlatform()` so web builds (Vercel) are untouched. On web, `StatusBar.*` would throw or no-op; the gate eliminates both paths.
- Use `Style.Dark` — research Pitfall 6: the style describes text, not background. Paper bg = dark text = `Style.Dark`. Do NOT use `Style.Light`.
- Wrap in try/catch with `console.warn('StatusBar setup skipped:', err)` so a plugin failure does not block React mount.
- The function is invoked but not awaited — it is fire-and-forget. React boots even if StatusBar setup fails.
- DO NOT modify App.jsx — the init happens at the module level in main.jsx so it runs exactly once at app boot.

2. Modify package.json `scripts` block — APPEND (do not replace) these entries:

       "cap:sync": "npm run build && npx cap sync",
       "cap:android": "npm run cap:sync && npx cap open android",
       "cap:ios": "npm run cap:sync && npx cap open ios"

Preserve existing scripts (`dev`, `build`, `preview`, `test`, `test:watch`).

3. Build + smoke test the web:

       npm run build
       npm run preview

Confirm the preview server boots and the AuthPage renders at http://localhost:4173 (default Vite preview port). The Capacitor.isNativePlatform() gate must keep StatusBar code from running — if you see "StatusBar setup skipped" in the browser console, the gate is broken (Capacitor 8 should return false on web; verify the import path is `@capacitor/core` not a typo). If the gate works, the console is silent and the page loads normally.

Deviation handling:
- If src/main.jsx already uses `import.meta.env.DEV` somewhere for dev-bypass auth, do NOT add a similar branch here — the runtime gate `Capacitor.isNativePlatform()` is the correct check (works in both dev and prod, distinguishes web from native independent of build mode).
- If the user has TypeScript-shaped imports in main.jsx (they should not — project is pure JS per CLAUDE.md), surface that and stop — TypeScript migration is out of scope per REQUIREMENTS.md.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/main.jsx imports `Capacitor` from `@capacitor/core`
    - src/main.jsx imports `StatusBar, Style` from `@capacitor/status-bar`
    - src/main.jsx contains an `if (!Capacitor.isNativePlatform()) return` early-exit
    - src/main.jsx calls `StatusBar.setStyle({ style: Style.Dark })` (NOT Style.Light)
    - src/main.jsx calls `StatusBar.setBackgroundColor({ color: '#FFF8F0' })`
    - src/main.jsx calls `StatusBar.setOverlaysWebView({ overlay: false })`
    - src/main.jsx wraps the calls in try/catch with `console.warn` on failure
    - The StatusBar init runs at module load (not inside a React component)
    - package.json scripts contain `cap:sync`, `cap:android`, `cap:ios`
    - `npm run build` succeeds
    - `npm run preview` boots and the page renders without console errors related to StatusBar
  </acceptance_criteria>
  <done>StatusBar plugin is initialized once at boot on native platforms only, with Style.Dark and the paper background color, while web builds remain untouched; npm convenience scripts let a developer build + sync + open the native projects with one command.</done>
</task>

<task type="auto">
  <name>Task 3: Create codemagic.yaml at repo root for iOS Capacitor build → TestFlight</name>
  <files>codemagic.yaml</files>
  <read_first>
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md (Pattern 7 — full yaml + Codemagic UI setup steps + Open Question 1 on triggering)
  </read_first>
  <action>
Atomic commit boundary.

1. Create codemagic.yaml at repo root with the exact structure described in the <interfaces> block (research Pattern 7). Use this skeleton, populated per <interfaces>:

       workflows:
         ios-capacitor-workflow:
           name: iOS Capacitor Build
           max_build_duration: 60
           instance_type: mac_mini_m2
           integrations:
             app_store_connect: theusquiz-ascapi
           environment:
             ios_signing:
               distribution_type: app_store
               bundle_identifier: com.theusquiz.app
             vars:
               XCODE_WORKSPACE: "ios/App/App.xcworkspace"
               XCODE_SCHEME: "App"
               BUNDLE_ID: "com.theusquiz.app"
             node: 20
             xcode: latest
             cocoapods: default
           triggering:
             events:
               - push
             branch_patterns:
               - pattern: main
           scripts:
             - name: Install npm deps
               script: npm ci
             - name: Build web bundle
               script: npm run build
             - name: Capacitor sync
               script: npx cap sync ios
             - name: Install CocoaPods
               script: cd ios/App && pod install && cd -
             - name: Set up keychain + fetch signing files
               script: |
                 keychain initialize
                 app-store-connect fetch-signing-files "$BUNDLE_ID" \
                   --type IOS_APP_STORE \
                   --create
                 keychain add-certificates
                 xcode-project use-profiles
             - name: Build IPA
               script: |
                 xcode-project build-ipa \
                   --workspace "$XCODE_WORKSPACE" \
                   --scheme "$XCODE_SCHEME"
           artifacts:
             - build/ios/ipa/*.ipa
             - /tmp/xcodebuild_logs/*.log
           publishing:
             app_store_connect:
               auth: integration
               submit_to_testflight: true

2. Add a leading comment block to the file explaining the operational model:

   - 500 macOS M2 minutes per month on free tier; each iOS Capacitor build is ~10–15 min so ~33 builds/month max
   - To pause auto-builds when minutes are tight, comment out the entire `triggering:` block and use the Codemagic UI to trigger manually
   - The `app-store-connect fetch-signing-files --create` line auto-creates a provisioning profile on first run; no manual Apple Developer Portal clicks needed
   - The `submit_to_testflight: true` line lands every successful build in TestFlight automatically — Phase 19 will refine this with version bumps and release notes

3. Do NOT trigger a Codemagic build at this step — the build runs in Task 5 (checkpoint) after operational prerequisites are confirmed in place.

Deviation handling:
- If Codemagic releases a yaml schema v2 between research date and execution date, validate the file against `codemagic.yaml --validate` if available — but the current schema is stable and the structure above has been used in production by other Capacitor projects.
- If John wants to switch from Codemagic to GitHub Actions macOS runners later (research A3), the same `app-store-connect fetch-signing-files` CLI works on GH Actions — the file maps cleanly to `.github/workflows/ios.yml`. Defer that swap; ship Codemagic for v2.0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('codemagic.yaml','utf8');const need=['ios-capacitor-workflow','theusquiz-ascapi','com.theusquiz.app','npx cap sync ios','pod install','xcode-project build-ipa','submit_to_testflight: true'];const miss=need.filter(s=>!y.includes(s));if(miss.length){console.error('Missing: '+miss.join(', '));process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - codemagic.yaml exists at repo root
    - File defines workflow `ios-capacitor-workflow`
    - File references `theusquiz-ascapi` integration name
    - File sets `bundle_identifier: com.theusquiz.app`
    - File includes script steps for: npm ci, npm run build, npx cap sync ios, pod install, fetch-signing-files, xcode-project build-ipa
    - File sets `submit_to_testflight: true` under publishing
    - File contains a leading comment about the 500-min/mo budget and how to disable auto-trigger
  </acceptance_criteria>
  <done>codemagic.yaml is committed at repo root with the exact build script ordering required for a Capacitor iOS build that signs via App Store Connect API and auto-submits to TestFlight.</done>
</task>

<task type="auto">
  <name>Task 4: Write BUILDING.md at repo root + Wave-0 build-docs test</name>
  <files>BUILDING.md, tests/build-docs.test.js</files>
  <read_first>
    - .planning/phases/14-capacitor-install-platform-setup/14-RESEARCH.md ("BUILDING.md outline" code example — full content)
    - package.json (confirm the npm scripts added in Task 2 actually exist before documenting them)
  </read_first>
  <action>
Atomic commit boundary.

1. Create BUILDING.md at repo root using the exact content in the research doc's "BUILDING.md outline" section (Code Examples → BUILDING.md outline). Required sections (the build-docs test asserts these as substrings):
   - `# Building The Us Quiz Native Apps`
   - `## Prerequisites` (subsections: All platforms, Android (local on Windows), iOS (Codemagic cloud))
   - `## Quick reference` (npm script table)
   - `## Android local build` (step-by-step: npm install, npm run cap:android, Android Studio signed build flow, output APK path)
     - Sub-subsection: "Generating the keystore (one-time)" with the `keytool` command from research Pattern 6 and the `keystore.properties` template — copy verbatim from research BUILDING.md outline. Include the "Back up the keystore" warning prominently.
   - `## iOS build via Codemagic` (push-to-main triggers, watch the build, TestFlight install)
     - Sub-subsection: "Manual iOS build trigger" (Codemagic UI → Recent builds → Start new build)
     - Sub-subsection: "Code signing setup (one-time)" — App Store Connect API key generation + upload to Codemagic, exactly as in research
   - `## Troubleshooting` with these four scenarios at minimum:
     - "White screen on launch" → check vite.config.js base path
     - "Status bar icons invisible" → confirm Style.Dark
     - "Codemagic build fails: No matching certificates found" → API key expired, regenerate
     - "Google Play: signature mismatch" → restore keystore or use Play App Signing
     - "Android 16: bottom nav hidden behind home indicator" → confirm adjustMarginsForEdgeToEdge + .bottom-nav safe-area-inset

2. Include a one-line note near the top: "All native build orchestration assumes John has no Mac. Android builds run locally on Windows. iOS builds run on Codemagic cloud CI."

3. Create tests/build-docs.test.js with substring assertions:

       import { describe, it, expect } from 'vitest'
       import fs from 'node:fs'
       import path from 'node:path'

       describe('BUILDING.md', () => {
         const doc = fs.readFileSync(path.resolve('BUILDING.md'), 'utf8')

         it('exists and is non-empty', () => {
           expect(doc.length).toBeGreaterThan(500)
         })
         it('has top-level title', () => {
           expect(doc).toMatch(/^#\s+Building The Us Quiz Native Apps/m)
         })
         it('has Prerequisites section', () => {
           expect(doc).toMatch(/##\s+Prerequisites/)
         })
         it('has Android local build section', () => {
           expect(doc).toMatch(/##\s+Android local build/)
         })
         it('has iOS build via Codemagic section', () => {
           expect(doc).toMatch(/##\s+iOS build via Codemagic/)
         })
         it('has Troubleshooting section', () => {
           expect(doc).toMatch(/##\s+Troubleshooting/)
         })
         it('documents the keytool command for keystore generation', () => {
           expect(doc).toContain('keytool')
           expect(doc).toContain('release.keystore')
         })
         it('documents the cap:android npm script', () => {
           expect(doc).toContain('cap:android')
         })
         it('warns about backing up the keystore', () => {
           expect(doc.toLowerCase()).toContain('back up')
         })
       })

4. Run `npm test` to confirm the test passes.

Deviation handling: If the research BUILDING.md outline lacks any troubleshooting case John has surfaced in prior phases (e.g., a known Vercel preview build issue), add a brief entry — but keep the doc focused on native build issues, not web-only ones.
  </action>
  <verify>
    <automated>npm test -- --run build-docs</automated>
  </verify>
  <acceptance_criteria>
    - BUILDING.md exists at repo root
    - BUILDING.md contains all required section headings listed above
    - BUILDING.md documents the `keytool` keystore-generation command
    - BUILDING.md mentions backing up the keystore (Pitfall 4)
    - BUILDING.md documents the `cap:android` and `cap:ios` npm scripts (matching the package.json additions from Task 2)
    - BUILDING.md notes that iOS builds run on Codemagic because John has no Mac
    - tests/build-docs.test.js exists and passes
  </acceptance_criteria>
  <done>BUILDING.md is a complete operator-ready guide for both platforms with a one-command build per platform, keystore management instructions, Codemagic setup, and a troubleshooting section covering the most likely failure modes; Wave-0 test guards the structural shape.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Run first Codemagic iOS build, install on TestFlight, verify safe-area on notched iPhone + Android emulator</name>
  <files>codemagic.yaml, src/index.css, src/main.jsx, BUILDING.md (no edits — runtime validation of NCAP-07 + NATIVE-08)</files>
  <action>
This task is GATED on the operational prerequisites listed in the `<prerequisites>` block at the top of this plan. If items 1–5 there are not yet complete, the executor pauses Task 5 and surfaces the gap to John. Tasks 1–4 already shipped autonomously; Task 5 cannot.

Executor pre-work:
1. Confirm with John that all Wave-0 prerequisites (Apple Developer enrollment, bundle ID reserved, App Store Connect API key uploaded to Codemagic as `theusquiz-ascapi`) are in place.
2. If yes: push the current branch to `main`. Codemagic will pick up the push and run `ios-capacitor-workflow`.
3. If no: surface the missing items, do not push, and ask John for direction (wait for prereqs, or skip this task and complete it in a follow-up commit once prereqs land).

For Android side of the checkpoint (always runnable on Windows, no iOS prerequisites):
1. From repo root: `npm run cap:android` → opens Android Studio with the freshly synced bundle.
2. Run on a Pixel emulator API 34+ (Android 14 or 15 — research Pitfall 7 calls out Android 16 enforcement; API 36 emulator is ideal if available).
3. Sign in, navigate to a page with the BottomNav visible (Vault, Quizzes hub).
4. Inspect: BottomNav sits above the gesture-bar gutter — not flush with the screen edge.
5. Inspect: paper background extends behind the status bar area at the top — no white strip.
6. Inspect: when scrolling, content does not get clipped under the status bar.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('codemagic.yaml'))throw new Error('codemagic.yaml missing — Task 3 incomplete');if(!fs.existsSync('BUILDING.md'))throw new Error('BUILDING.md missing — Task 4 incomplete');console.log('Plan 02 artifacts in place — ready for Codemagic push + device install')"</automated>
  </verify>
  <what-built>
    iOS: Codemagic-built IPA installed via TestFlight on a real iPhone (notched device — iPhone X+ generation).
    Android: Capacitor-wrapped APK installed on an emulator running API 34+ (Android 14/15 — Android 16 emulator if available).
    Both verify: paper background extends into status bar, status bar icons render as dark on light, BottomNav stays above the home indicator / gesture bar, no UI element clips under the safe-area chrome.
  </what-built>
  <how-to-verify>
    **iOS half (after Codemagic build succeeds):**
    1. Open TestFlight on the iPhone → wait for "The Us Quiz" build to appear (Codemagic auto-submits via `submit_to_testflight: true`) → tap Install.
    2. Launch the app. Expect AuthPage to render.
    3. Verify the status bar area at the top: the warm paper bg (`#FFF8F0`) extends edge-to-edge across the status bar — there is NO black bar or white strip above the WebView content.
    4. Verify status bar icons (time, wifi, battery) are visible as DARK glyphs on the light bg. If they are invisible or white, the StatusBar.setStyle call is wrong — go back to src/main.jsx and confirm `Style.Dark` (not `Style.Light` — research Pitfall 6).
    5. Navigate to a page with the BottomNav (Vault). Verify the BottomNav sits above the home indicator on iPhone X+ — there is a visible paper-colored gap between the BottomNav and the screen bottom edge, accounting for `env(safe-area-inset-bottom)`.
    6. Sign in (existing prod auth flow). Verify auth works — this is bonus context for Phase 15 but worth confirming.
    7. Force-quit and relaunch. App boots from the bundle — instant cold-launch with no white flash longer than the splash screen (default Capacitor blue splash is acceptable here; paper-themed splash lands in Phase 18).

    **Android half (runnable independently on John's Windows box):**
    1. Boot a Pixel 6+ emulator at API 34 (Android 14) or API 35+ (Android 15) — use Android Studio's Device Manager.
    2. From repo root: `npm run cap:android` → Android Studio opens → Run on emulator.
    3. Verify paper bg extends behind the status bar (same expectation as iOS — research Pattern 4).
    4. Verify status bar icons render dark on light.
    5. Navigate to a page with the BottomNav. Verify BottomNav stays above the gesture-bar gutter (paper gap below it, accounting for `env(safe-area-inset-bottom)`).
    6. If an API 36 (Android 16) emulator image is available, repeat the check there — Android 16 enforces edge-to-edge, which is the most likely failure mode (research Pitfall 7).
  </how-to-verify>
  <done>User confirms both: (a) Codemagic-built IPA installed via TestFlight shows paper-themed status bar with dark icons and no clipping on a notched iPhone, AND (b) Android emulator (API 34+) shows the same safe-area behavior with BottomNav above the gesture bar.</done>
  <resume-signal>Type "approved" with one-line notes for each platform (which device/emulator + what you verified), OR list specific visual issues (e.g., "status bar icons invisible on iPhone 14", "BottomNav clipped on Pixel 7 API 35") so the plan can be corrected before merging.</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes including tests/viewport-meta.test.js, tests/safe-area-css.test.js, tests/build-docs.test.js
- index.html viewport meta contains `viewport-fit=cover`
- src/index.css has env(safe-area-inset-top) on .page and html/body paper bg
- src/main.jsx calls StatusBar.setStyle/setBackgroundColor/setOverlaysWebView, gated on Capacitor.isNativePlatform()
- codemagic.yaml exists with ios-capacitor-workflow → fetch-signing-files → build-ipa → submit_to_testflight: true
- BUILDING.md exists with Prerequisites, Android local build, iOS via Codemagic, Troubleshooting sections
- package.json has cap:sync / cap:android / cap:ios scripts
- Manual UAT confirms paper-themed status bar + safe-area behavior on a real iPhone via TestFlight and on Android emulator (Task 5)
</verification>

<success_criteria>
Maps to ROADMAP.md Phase 14 Success Criteria 1, 2, 4, 5 (per the table at the bottom of this plan).

- `npm run build && npx cap sync` followed by `npm run cap:android` opens Android Studio; push-to-main triggers Codemagic to build the iOS IPA (SC1 — full closure across both plans)
- Installed iOS app loads from bundled assets — verified at Task 5 TestFlight install + no theusquiz.com asset requests (SC2)
- Notched iPhone (via TestFlight) and modern Android device show paper bg in status bar, no UI clipping, BottomNav above home indicator (SC4)
- BUILDING.md lets a developer reproduce both builds from a clean checkout (SC5)
</success_criteria>

<output>
After completion, create `.planning/phases/14-capacitor-install-platform-setup/14-02-SUMMARY.md`
</output>

---

## Success Criteria Mapping

| ROADMAP Success Criterion | Tasks |
|---------------------------|-------|
| SC1: `npm run build && npx cap sync` followed by one command opens a working app in Xcode and in Android Studio | Task 2 (npm scripts cap:sync / cap:android / cap:ios), Task 3 (codemagic.yaml replaces "open Xcode" with "push to main"), Task 4 (BUILDING.md documents both flows). Plan 01 supplied the underlying cap install + native project skeletons. |
| SC2: Installed iOS app loads from bundled assets | Task 3 (codemagic.yaml uses `npx cap sync ios` then `xcode-project build-ipa` — no remote URL involved), Task 5 (TestFlight install verifies on a real iPhone). Plan 01 ensured no server.url in capacitor.config.json. |
| SC3: Installed Android app loads from bundled assets | Plan 01 Task 4 (Chrome DevTools verification on Android emulator) — fully closed in Plan 01. |
| SC4: Safe-area + status-bar handling on notched devices | Task 1 (viewport-fit=cover + safe-area-inset-top CSS + html/body paper bg), Task 2 (StatusBar.setStyle Dark + setBackgroundColor paper), Task 5 (visual verification on iPhone via TestFlight + Android emulator). Plan 01 set `adjustMarginsForEdgeToEdge: 'auto'` in capacitor.config.json. |
| SC5: BUILDING.md lets a developer reproduce both builds | Task 4 (BUILDING.md authored + Wave-0 test guards structural shape). |
