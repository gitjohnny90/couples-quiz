---
phase: 06-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: ["06-01", "06-02"]
files_modified:
  - src/pages/ResultsPage.jsx
  - src/pages/DrawResultsPage.jsx
autonomous: true
gap_closure: true
requirements: [BUG-01]

must_haves:
  truths:
    - "Waiting screen shows invite code when partner has not joined the session"
    - "Waiting screen shows 'waiting on your partner' message when partner has joined but hasn't completed the activity"
    - "No /join/ URL is ever displayed on a results or draw-results waiting screen"
  artifacts:
    - path: "src/pages/ResultsPage.jsx"
      provides: "Session-aware waiting screen with two sub-states"
      contains: "from('sessions')"
    - path: "src/pages/DrawResultsPage.jsx"
      provides: "Session-aware waiting screen with two sub-states"
      contains: "from('sessions')"
  key_links:
    - from: "src/pages/ResultsPage.jsx"
      to: "sessions table"
      via: "supabase query for player2_name/player2_user_id/invite_code"
      pattern: "from\\('sessions'\\).*select.*invite_code"
---

<objective>
Replace the unconditional share-link waiting screens on ResultsPage and DrawResultsPage with session-aware two-state waiting screens.

Purpose: Users reported that showing a /join/ URL on a results page makes no sense. The waiting screen should show the invite code (LOVE-XXXX) if the partner hasn't joined yet, or a simple "waiting on your partner" message if the partner has joined but hasn't completed the activity.
Output: Both pages fetch session data and render the correct sub-state.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bug-fixes/06-01-SUMMARY.md

<interfaces>
<!-- Pattern from other pages that already fetch session data -->
From src/pages/HotTakesPage.jsx (line 74):
```javascript
.from('sessions').select('player1_name, player2_name').eq('id', sessionId).single()
```

From src/pages/VaultPage.jsx (line 34):
```javascript
let { data: sessionData } = await supabase.from('sessions').select('id, player1_name, player2_name, invite_code').eq('id', sessionId).single()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session-aware waiting sub-states to ResultsPage</name>
  <files>src/pages/ResultsPage.jsx</files>
  <action>
In ResultsPage.jsx, replace the unconditional share-link waiting block (lines ~138-173) with a session-aware two-state waiting screen:

1. Add a `sessionInfo` state variable: `const [sessionInfo, setSessionInfo] = useState(null)`

2. Add a session fetch inside the existing `fetchResponses` useCallback (or as a separate useEffect on mount). Query:
   ```javascript
   const { data: sess } = await supabase
     .from('sessions')
     .select('player2_name, player2_user_id, invite_code')
     .eq('id', sessionId)
     .single()
   if (mountedRef.current && sess) setSessionInfo(sess)
   ```

3. Remove the `shareUrl` variable and `copyLink` function entirely (lines ~109-115).

4. Replace the waiting block (`if (responses.length < 2)`) with TWO sub-states:

   **Sub-state A — Partner has NOT joined** (`!sessionInfo?.player2_name && !sessionInfo?.player2_user_id`):
   - Show heading: "waiting for your person..."
   - Show subtext: "share this code so they can join:"
   - Show the invite code in a styled div: `{sessionInfo?.invite_code || '...'}` (use same styling as HomePage invite code display: large font-hand text, coral color, centered)
   - Add a "copy code" button that copies `sessionInfo?.invite_code` to clipboard (same copied/setCopied pattern already in the file)
   - Keep the "check again" button

   **Sub-state B — Partner has joined but hasn't answered** (`sessionInfo?.player2_name || sessionInfo?.player2_user_id`):
   - Show heading: "waiting for your person..."
   - Show subtext: "they need to answer the same questions first!"
   - Show the "check again" button
   - Do NOT show any link or code

5. If `sessionInfo` is still null (loading), show the generic "waiting for your person..." without link or code (safe default).

Keep the existing PageDoodles, DoodleCloud, motion wrapper, and glass card styling. Just change the CONTENT inside the waiting block.
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ResultsPage waiting screen shows invite code when partner hasn't joined, shows "waiting on partner" message when partner has joined but hasn't answered. No /join/ URL is displayed.</done>
</task>

<task type="auto">
  <name>Task 2: Add session-aware waiting sub-states to DrawResultsPage</name>
  <files>src/pages/DrawResultsPage.jsx</files>
  <action>
Apply the same two-state waiting pattern from Task 1 to DrawResultsPage.jsx:

1. Add `sessionInfo` state variable.

2. Fetch session data (player2_name, player2_user_id, invite_code) from sessions table. Add to existing fetchResponses or as separate mount effect.

3. Remove `shareUrl` variable and `copyLink` function (lines ~94-100).

4. Replace the waiting block (`if (!bothDone)`, lines ~119-176) with the same two sub-states:
   - Sub-state A (no partner): Show invite code + copy button
   - Sub-state B (partner joined, no drawing): Show "they need to draw their masterpiece too!" + check again button

5. Keep the existing "draw another" button that appears when `promptId` is present (lines ~165-173) — it should show in BOTH sub-states.

Keep all existing styling, PageDoodles, motion wrappers. Just replace the share link content.
  </action>
  <verify>
    <automated>cd C:/Users/mcfat/projects/couples-quiz && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DrawResultsPage waiting screen shows invite code when partner hasn't joined, shows "waiting on partner" when partner has joined. No /join/ URL displayed. "Draw another" button still appears when promptId exists.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- ResultsPage.jsx contains `from('sessions')` query
- DrawResultsPage.jsx contains `from('sessions')` query
- Neither file contains `/join/` URL construction
- Both files reference `invite_code` from session data
</verification>

<success_criteria>
- Both waiting screens show invite code (LOVE-XXXX) when partner hasn't joined
- Both waiting screens show "waiting" message (no link/code) when partner has joined but hasn't completed
- No /join/ URL appears on either page
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fixes/06-03-SUMMARY.md`
</output>
