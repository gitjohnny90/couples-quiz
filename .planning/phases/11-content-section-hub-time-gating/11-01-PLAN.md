---
phase: 11-content-section-hub-time-gating
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/data/photoSections.js
  - src/utils/photoGating.js
  - src/data/pageGuides.js
  - src/utils/sessionUtils.js
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "15 themed sections exist with 3 prompts each (45 total)"
    - "First prompt of every section is 'What are you up to?'"
    - "Last prompt of every section is a funny/unhinged question"
    - "Sections cover a variety of themes (food, travel, work, etc.)"
    - "isGloballyFrozen returns true after completion and before 6am next day"
    - "isGloballyFrozen returns false after 6am next day"
    - "getSectionStatus returns 'locked-in-progress' for non-active sections when one is picked"
    - "getSectionStatus returns 'available' for all incomplete sections when gate is open"
  artifacts:
    - path: "src/data/photoSections.js"
      provides: "15 themed section objects with prompts"
      exports: ["default"]
    - path: "src/utils/photoGating.js"
      provides: "Pure time-gating functions"
      exports: ["next6amAfter", "isGloballyFrozen", "frozenUntil", "getSectionStatus"]
  key_links:
    - from: "src/utils/photoGating.js"
      to: "responses table JSONB shape"
      via: "state parameter convention"
      pattern: "completedSections.*lastCompletedAt.*inProgressSectionId"
---

<objective>
Create the 15 themed photo sections data file and the pure time-gating utility functions, with TDD coverage for all gating boundary logic and data integrity constraints.

Purpose: These are the foundational data and logic modules that Plan 02's hub page will consume. The gating functions are where the tricky boundary conditions live (6am cutoff, timezone handling), so TDD is essential. The data file's section IDs become permanent keys used in Storage paths and DB state.

Output: `src/data/photoSections.js` (static data), `src/utils/photoGating.js` (pure functions), plus updates to `sessionUtils.js` (route registration) and `pageGuides.js` (onboarding content).
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-content-section-hub-time-gating/11-RESEARCH.md
@.planning/phases/11-content-section-hub-time-gating/11-UI-SPEC.md
@src/data/deepDiveDecks.js (pattern reference for data file shape)
@src/utils/sessionUtils.js (add /daily-photos route support)
@src/data/pageGuides.js (add dailyPhotos entry)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create photoSections.js data file and photoGating.js utility with TDD</name>
  <files>src/data/photoSections.js, src/utils/photoGating.js, src/data/photoSections.test.js, src/utils/photoGating.test.js</files>
  <behavior>
    - photoSections exports array of exactly 15 objects
    - Each section has: id (string), title (string), emoji (string), description (string), prompts (array of 3)
    - Each prompt has: id (string), text (string)
    - prompts[0].text is always "What are you up to?" for all 15 sections
    - All section IDs are unique, kebab-case, URL-safe
    - All prompt IDs are unique across all sections
    - isGloballyFrozen returns false when state is null/empty/no lastCompletedAt
    - isGloballyFrozen returns true when completed at 2pm and now is 11pm same day
    - isGloballyFrozen returns true when completed at 11pm and now is 5:59am next day
    - isGloballyFrozen returns false when completed at 2pm and now is 7am next day
    - isGloballyFrozen returns false when completed at 5:59am and now is 6:01am next day (edge case: complete just before 6am, unlock 6am same "next day")
    - getSectionStatus returns "completed" for a section in completedSections
    - getSectionStatus returns "in-progress" for the inProgressSectionId
    - getSectionStatus returns "locked-frozen" when globally frozen and section is incomplete
    - getSectionStatus returns "locked-in-progress" when another section is in progress
    - getSectionStatus returns "available" when gate is open and no section in progress
    - frozenUntil returns null when not frozen
    - frozenUntil returns ISO string of next 6am when frozen
    - next6amAfter returns 6am the calendar day after the given timestamp (local time)
  </behavior>
  <action>
    RED phase: Create test files first.

    `src/data/photoSections.test.js`:
    - Import photoSections (default export)
    - Test: exactly 15 sections
    - Test: each section has id, title, emoji, description, prompts with length 3
    - Test: every prompts[0].text === "What are you up to?"
    - Test: all section IDs unique and match /^[a-z0-9-]+$/
    - Test: all prompt IDs unique across all sections

    `src/utils/photoGating.test.js`:
    - Import { next6amAfter, isGloballyFrozen, frozenUntil, getSectionStatus }
    - Test all behaviors listed above using injectable `now` parameter
    - Use fixed Date objects (not Date.now()) so tests are deterministic
    - For the 6am boundary test: create a date at 2pm, then test with now at 5:59am next day (frozen) and 6:01am next day (unfrozen)
    - Edge case: completed at 5:59am — next6am should be 6am SAME DAY (just 1 minute later), so unfrozen almost immediately

    Run tests — they MUST fail (modules don't exist yet).

    GREEN phase: Create the production files.

    `src/data/photoSections.js`:
    - Default export of 15 section objects following the UI-SPEC table exactly:
      1. morning-routine, 2. current-meal, 3. right-now-vibes, 4. your-space, 5. outside-right-now,
      6. date-night, 7. pet-or-plant, 8. self-care-day, 9. working-on-it, 10. travel-mode,
      11. cooking-or-ordering, 12. getting-ready, 13. weekend-energy, 14. something-new, 15. end-of-day
    - Each section: { id, title, emoji, description, prompts: [{ id, text }, { id, text }, { id, text }] }
    - Prompt IDs: `{sectionId}-1`, `{sectionId}-2`, `{sectionId}-3`
    - prompts[0].text ALWAYS "What are you up to?" (CONT-02)
    - prompts[2].text is the funny/unhinged question from UI-SPEC table (CONT-03)
    - Add comment at top: "// Section IDs are permanent keys used in Storage paths and DB state. NEVER rename after launch."

    `src/utils/photoGating.js`:
    - Export: next6amAfter(completedAt) — returns Date of 6:00:00.000 AM on the day after completedAt, in local time
    - Export: isGloballyFrozen(state, now = new Date()) — returns boolean
    - Export: frozenUntil(state, now = new Date()) — returns ISO string or null
    - Export: getSectionStatus(sectionId, state, now = new Date()) — returns one of: 'completed', 'in-progress', 'available', 'locked-in-progress', 'locked-frozen'
    - All functions are pure — no side effects, no imports, no Supabase calls
    - State shape: { completedSections: { [sectionId]: isoTimestamp }, inProgressSectionId: string|null, lastCompletedAt: string|null }
    - CRITICAL timezone note: next6amAfter uses `new Date(completedAt)` then `.setDate(d+1)` then `.setHours(6,0,0,0)`. This operates in LOCAL time which is correct — we want 6am on the user's clock.

    Run tests — they MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/data/photoSections.test.js src/utils/photoGating.test.js</automated>
  </verify>
  <done>
    - photoSections.js exports 15 sections with 3 prompts each (45 total)
    - Every first prompt is "What are you up to?"
    - photoGating.js exports 4 pure functions with correct 6am boundary logic
    - All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Register route in sessionUtils.js and add page guide entry</name>
  <files>src/utils/sessionUtils.js, src/data/pageGuides.js</files>
  <action>
    In `src/utils/sessionUtils.js`:
    1. In `isTabActive`, inside the `if (base === '/vault')` block, add `pathname.startsWith('/daily-photos')` to the return chain (after the `/quiz-packs` line). Also add `/daily-photo-section` and `/daily-photo-reveal` for Phase 12 forward-compatibility.
    2. In `getDocumentTitle`, add a line before the `/fun` check: `if (pathname.startsWith('/daily-photos') || pathname.startsWith('/daily-photo-section') || pathname.startsWith('/daily-photo-reveal')) return 'Daily Photo Challenge — The Us Quiz'`

    In `src/data/pageGuides.js`:
    Add the `dailyPhotos` entry to the pageGuides object (after the `home` entry):
    ```
    dailyPhotos: {
      title: 'Daily Photos',
      lines: [
        'Pick a theme and take 3 photos together — one for each prompt.',
        'You can only do one section per day. After you finish, the rest unlock again at 6am tomorrow.',
        "Once you pick a theme, it locks in until you're done — no switching mid-section!"
      ]
    }
    ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - isTabActive recognizes /daily-photos as part of the vault/quizzes tab group
    - getDocumentTitle returns correct title for /daily-photos routes
    - pageGuides has dailyPhotos entry with 3 lines of onboarding copy
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (photoSections data integrity + photoGating boundary logic)
2. `npm run build` — production build succeeds with no errors
3. photoSections.js has exactly 15 entries, each with 3 prompts
4. photoGating.js exports 4 named functions with no side effects
</verification>

<success_criteria>
- 15 themed sections with correct prompt structure (CONT-01, CONT-02, CONT-03, CONT-04)
- Time-gating logic correctly handles 6am boundary, global freeze, and section locking (GATE-01, GATE-02, GATE-03)
- All unit tests pass
- Route utilities updated for Phase 11 + Phase 12 routes
- Page guide entry ready for hub page
</success_criteria>

<output>
After completion, create `.planning/phases/11-content-section-hub-time-gating/11-01-SUMMARY.md`
</output>
