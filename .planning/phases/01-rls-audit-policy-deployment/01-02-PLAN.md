---
phase: 01-rls-audit-policy-deployment
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06]
user_setup:
  - service: supabase
    why: "Run SQL migrations in Supabase SQL Editor"
    dashboard_config:
      - task: "Run audit SQL, then indexes SQL, then policies SQL in order"
        location: "Supabase Dashboard -> SQL Editor"

must_haves:
  truths:
    - "RLS is enabled on all 11 tables in the Supabase project"
    - "Both partners can read and write all feature data for their shared session"
    - "Neither partner can read data from a different couple's session"
    - "Player2 can access every page and see the same data as Player1"
    - "Realtime subscriptions deliver partner updates after RLS is enabled"
    - "Drawing data in responses table is protected (SEC-04 covered)"
  artifacts:
    - path: "supabase-rls-audit.sql"
      provides: "Audit results confirming pre-deployment state"
    - path: "supabase-rls-indexes.sql"
      provides: "Performance indexes deployed"
    - path: "supabase-rls-policies.sql"
      provides: "All RLS policies deployed and active"
  key_links:
    - from: "Supabase RLS policies"
      to: "App feature pages"
      via: "Authenticated REST API calls"
      pattern: "supabase.from.*select|upsert|insert"
---

<objective>
Deploy the RLS SQL migrations to Supabase and verify that both partners can still use the app while cross-session access is blocked.

Purpose: The SQL files from Plan 01 are ready but need to be executed in the Supabase SQL Editor (Claude cannot access the dashboard). After deployment, verify that RLS is correctly enforced for all requirements.

Output: All 11 tables have RLS enabled with correct policies. App continues to work for both partners. Cross-session data is inaccessible.
</objective>

<execution_context>
@C:/Users/mcfat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mcfat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rls-audit-policy-deployment/01-RESEARCH.md
@.planning/phases/01-rls-audit-policy-deployment/01-01-SUMMARY.md
@supabase-rls-audit.sql
@supabase-rls-indexes.sql
@supabase-rls-policies.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run SQL migrations in Supabase SQL Editor</name>
  <files>supabase-rls-audit.sql, supabase-rls-indexes.sql, supabase-rls-policies.sql</files>
  <action>
User must execute the three SQL files in the Supabase SQL Editor in this exact order. Claude cannot access the Supabase dashboard.

Execute these three SQL files in the Supabase Dashboard SQL Editor (https://supabase.com/dashboard > your project > SQL Editor), in this exact order:

Step 1 — Audit current state:
- Open supabase-rls-audit.sql, copy its contents into the SQL Editor, and run it
- Review the results: note which tables already have rowsecurity=true and any existing policies
- If any stale policies exist, DROP them before proceeding (the audit file explains what to look for)

Step 2 — Deploy indexes:
- Open supabase-rls-indexes.sql, copy its contents into the SQL Editor, and run it
- All statements use IF NOT EXISTS so this is safe to re-run
- Expected: 11 indexes created (or already exist)

Step 3 — Deploy RLS policies:
- Open supabase-rls-policies.sql, copy its contents into the SQL Editor, and run it
- Each table section is wrapped in BEGIN/COMMIT for atomicity
- If any section fails, the transaction rolls back for that table only — check the error and fix before retrying
- Expected: 11 tables enabled, all policies created

Step 4 — Quick smoke test:
- Open the app in your browser (logged in as either partner)
- Navigate to the vault page — verify quiz data still loads
- If pages show empty data, something went wrong — check the SQL Editor output for errors

Resume: Type "deployed" when all three SQL files have been run successfully, or describe any errors you encountered.
  </action>
  <verify>User confirms "deployed" with no errors</verify>
  <done>All three SQL files executed successfully in Supabase SQL Editor. Quick smoke test shows data still loads.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify RLS enforcement for both partners and cross-session isolation</name>
  <files>supabase-rls-policies.sql</files>
  <action>
Row Level Security policies are now deployed on all 11 Supabase tables. Both partners should be able to use the app normally. Cross-session data should be inaccessible. Verify with the following tests:

Test 1 — Both partners can access data (SEC-01, SEC-03):
1. Open the app in Browser A, signed in as Player 1
2. Open the app in Browser B (or incognito), signed in as Player 2
3. Navigate to the vault in both browsers — both should see quiz packs and any existing results
4. Navigate to a completed quiz's results page — both should see both partners' answers
5. Check the profiles page — both should see profile data

Test 2 — Realtime still works (SEC-06):
1. In Browser A (Player 1), go to a quiz and submit an answer
2. In Browser B (Player 2), verify the answer appears within 2 seconds (realtime) without manual refresh
3. If it appears within 5 seconds but not 2, realtime may be broken (polling fallback is catching it)

Test 3 — Cross-session isolation (SEC-01):
1. Open browser dev tools Network tab
2. Find any Supabase REST API call (look for rest/v1/ in the URL)
3. Copy the Authorization header (Bearer token)
4. In the browser console, run:
   fetch('https://YOUR_PROJECT.supabase.co/rest/v1/responses?session_id=eq.FAKE-SESSION-ID', {
     headers: { 'apikey': 'YOUR_ANON_KEY', 'Authorization': 'Bearer YOUR_JWT' }
   }).then(r => r.json()).then(console.log)
5. Expected result: empty array [] (not an error, just no data)
6. If you get actual rows back, the policies are not working

Test 4 — SEC-04 confirmation:
Drawing data is stored in the responses table as base64 JSONB — if Test 1 passes for responses, SEC-04 is covered. No separate Storage verification needed.

Resume: Type "approved" if all tests pass, or describe which test failed and what you observed.
  </action>
  <verify>User confirms "approved" — all 4 tests pass</verify>
  <done>Both partners can access their shared session data. Cross-session API calls return empty arrays. Realtime events arrive within 2 seconds. SEC-04 covered by responses table policy.</done>
</task>

</tasks>

<verification>
All 6 security requirements verified:
- SEC-01: Cross-session API call returns empty array
- SEC-02: Policies use (SELECT auth.uid()) — verified in SQL file authoring (Plan 01)
- SEC-03: Player2 can see session data and partner names
- SEC-04: Drawing data protected by responses table policy
- SEC-05: Atomic deployment via BEGIN/COMMIT blocks
- SEC-06: Realtime events arrive within 2 seconds after RLS enabled
</verification>

<success_criteria>
Both partners can use every feature page. Cross-session REST API calls return empty arrays. Realtime subscriptions deliver partner updates within 2 seconds. No pages show empty data or errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-rls-audit-policy-deployment/01-02-SUMMARY.md`
</output>
